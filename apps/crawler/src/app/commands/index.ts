import { cleanInvalidPictures } from './cleanInvalidPictures';
import { fetchBeachesData } from './fetchBeachesData';
import { processPicture } from './processPicture';
import { processPictures } from './processPictures';

export const commands = {
  cleanInvalidPictures,
  processPicture,
  processPictures,
  fetchBeachesData,
};
