import { freshProcess } from './freshProcess';
import { process } from './process';
import { processPicture } from './processPicture';

export const commands = {
  freshProcess,
  process,
  processPicture,
};
