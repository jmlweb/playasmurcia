# UI review directives

**Agent rules:** [AGENTS.md](../../../AGENTS.md) — *Reports, UI reviews, and audits (mandatory for agents)*.

| Path | Role |
|------|------|
| `docs/dev/ui-review/{name}.md` | Directives during an **active** `/ui-review` run (before tasks exist). |
| `docs/dev/ui-review/processed/{name}.md` | Directives **after** backlog tasks link to them. |

Workflow steps: [`.claude/templates/ui-review.md`](../../../.claude/templates/ui-review.md).
